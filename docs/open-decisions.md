# Open Decisions

| ID | Decision | Options | Current Lean | Needed By | Status |
| --- | --- | --- | --- | --- | --- |
| OD-001 | Final object ID allocation | Use 88100-88149, request a larger range, split into app modules | Use current `app.json` range 88100-88149 for Phase 1 | Initial implementation baseline | Decided |
| OD-002 | Target BC version | Target 28.0 only, support older BC, support latest only | Target Business Central 2026 release wave 1 / version 28.0 with runtime 17.0 | Before first compile | Decided |
| OD-003 | Default posted table policy | Deny all, allow selected posted tables, allow by approval | Deny all until explicitly allow-listed. Foundation policy records default to blocked. | Before policy code | Decided |
| OD-004 | Approval model among `SUPER` users | No approval, same `SUPER` user confirmation, second `SUPER` user approval, dual control, external workflow | Configurable approval model. Approval with a separate `SUPER` approver remains the safer default for high-risk work, but setup can disable approval for standard requests or allow self-approval for one-person companies that accept the control tradeoff. Foundation records store requester, approval requirement, separate-approver requirement, and approver when approval is used. | Before workflow code | Decided |
| OD-005 | Validation mode | Always validate field triggers, allow raw assignment, per-policy mode | Per-policy mode with safe default of validate triggers. Raw assignment remains blocked until a later readiness slice explicitly clears it. | Before execution code | Decided |
| OD-006 | Field type support | Text/numeric/date only, broad scalar support, include BLOB/media | Start with scalar fields; defer BLOB/media and complex types until sandbox validation. | Before value serializer | Decided |
| OD-007 | Rollback conflict policy | Stop on conflict, allow override, auto-merge | Stop on conflict by default. Override is a future policy-controlled exception. | Before rollback code | Decided |
| OD-008 | Audit metadata retention | Permanent, configurable years, external archive | User-configurable with conservative default and optional never-delete policy. Foundation default is 3650 days. | Before data model code | Decided |
| OD-009 | Export format | Excel, CSV, API, report layout | Excel/CSV for `SUPER` reviewer export; no external API in Phase 1. | Before audit export | Decided |
| OD-010 | Environment safety | Same behavior everywhere, sandbox-only first, production gated | Local implementation may proceed continuously. Sandbox and production use remain gated by environment validation, runtime controls, and release readiness evidence. | Before deployment | Decided |
| OD-011 | Sensitive value display | Full values to `SUPER` users, channel-based redaction, never show values | `SUPER`-only UI with export/log redaction. Generic logs and telemetry never contain full values. | Before UI code | Decided |
| OD-013 | `SUPER` access enforcement | Runtime check, object access through `SUPER` only, both runtime and object access | Use both runtime `User Permissions`.IsSuper(UserSecurityId()) and no BCDA permission set objects. | Initial implementation baseline | Decided |
| OD-012 | External API | No API, admin API, integration API | External API code may be prototyped only behind disabled runtime controls with `SUPER`, policy, audit, tenant, redaction, and rate controls. Runtime availability remains disabled until a future release decision and validation evidence allow it. | Before API code | Decided |
| OD-014 | Default rollback snapshot logging | Enabled for all, enabled only for high-risk, disabled by default, policy controlled | Policy controlled; required by default for posted/high-risk data. Foundation setup default is required. | Before setup/policy code | Decided |
| OD-015 | Operation retention implementation | Business Central retention policies, custom cleanup, both | Prefer Business Central retention policies for BCDA-owned tables using `Reten. Pol. Allowed Tables`; custom cleanup only if platform gaps remain. | Before retention code | Decided |
| OD-016 | Minimum retention periods | No minimum, 30/90/365-day defaults, customer-defined minimums | Foundation defaults: audit metadata 3650 days, rollback snapshots 90 days, technical logs 30 days. Minimums and never-delete handling remain visible setup/business policy choices before release. | Before setup code | Decided |
| OD-017 | Target record identity and complex keys | Free-text serialized key, platform `RecordId`, custom key table, target-record matrix selector | Use platform `RecordId` plus company context and a display key. Correction lines should be maintained through a Dimension Matrix-style selector/editor so users do not hand-enter composite primary keys. Local implementation can proceed; production reliance waits for sandbox validation. | Before target record discovery code | Decided |
| OD-018 | Insert grouping identity | Group by empty RecordId, one insert per line, app-owned insert group id, target matrix creates insert groups | Insert execution implementation may proceed using an operation-specific app-owned grouping design. `Insert` must keep target `RecordId` empty, and runtime availability must stay blocked until grouping, audit, rollback/unavailable-state, and policy behavior are implemented and validated. | Before insert execution code | Decided for local implementation |
| OD-019 | Setup-controlled data policy enforcement | Always enforce data policies, add `Allow Data Policies` to choose whether policy records participate, add `Enforce Data Policies` with clearer inverse semantics, allow all non-BCDA tables when policies are disabled | Implemented as `Allow Data Policies`, enabled by default. When disabled, policy records are bypassed, but BCDA app-owned tables, unsupported fields, non-`SUPER` access, missing required request metadata, unaudited mutation, and rollback snapshot controls remain enforced. | Phase 6 execution code | Decided |
| OD-020 | Continuous implementation authorization | Per-phase confirmation, standing authorization with runtime gates, fully ungated development | Standing authorization with runtime gates. Code development may proceed without separate paper confirmation for each phase when it follows the SDD and preserves user review, data policies, `SUPER`, audit, redaction, rollback, tests, and production validation controls. | Before next implementation task | Decided |
